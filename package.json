{
  "name": "os-exchange",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://pdeploy.hg.network --ipfs https://f.hg.network ijs/os-exchange",
    "create-local": "graph create --node http://localhost:8020/ ijs/os-exchange",
    "remove-local": "graph remove --node http://localhost:8020/ ijs/os-exchange",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ijs/os-exchange"
  },
  "dependencies": {
    "@hgdotnetwork/graph-cli": "0.20.4",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
